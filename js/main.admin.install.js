require(["dojo/parser", 
         "dojo/ready", 
         "dijit/form/TextBox", 
         "dijit/form/Button"],
    function(parser, ready){
        ready(function(){
            parser.parse();
        });
    });